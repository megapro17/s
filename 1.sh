mkdir -p ~/.ssh && chmod 700 ~/.ssh && touch ~/.ssh/authorized_keys && chmod 600 ~/.ssh/authorized_keys && KEYS=$(curl -fsSL https://github.com/megapro17.keys) && [ "$KEYS" != "$(cat ~/.ssh/authorized_keys)" ] && printf "%s\n" "$KEYS" > ~/.ssh/authorized_keys
